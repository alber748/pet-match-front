export interface Dog {
      id: number;
      idPersona : number;
      name: string;
      edad: string;
      peso: string;
      files: File[];
      descripcion: string;
      situacion: string;
}